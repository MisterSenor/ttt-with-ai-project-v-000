#!/usr/bin/env ruby

require_relative '../config/environment'

### welcome the user

### play through a game

### ask the user if they want to play again
   ### if yes then play again
	   ### loop back to ask if they want to play again
   ### if no then exit program with a goodbye message

puts "Welcome to Tic Tac Toe!"
puts "\n"
game = Game.new
game.board.display
game.play
if game.draw?
  puts "Would you like to play again?"
  input = gets.strip
  if input == "Y" || input == "y"
    game = Game.new
    game.board.display
    end
  end



# puts "Would you like to play again?"
# input = gets.strip
# if input == "Y" || input == "y"
#   game = Game.new
#   until game.won? || game.over?
#     game.play
#   end
# end
